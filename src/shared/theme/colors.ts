// Variante nativa: los componentes reciben directamente los valores hex
// reales. Metro resuelve `colors.web.ts` en su lugar al compilar para web
// (ver ese archivo para el porqué).
export { lightColors, darkColors } from './colorRoles';
export type { ThemeColors } from './colorRoles';
